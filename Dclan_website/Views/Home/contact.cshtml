<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Qadryn - Contact Us</title>
    <!-- Inter Font -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <!-- Font Awesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* --- RESET & VARIABLES --- */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        :root {
            --primary-purple: #6366F1; /* The main button color */
            --primary-hover: #4F46E5;
            --text-dark: #1F1F38;
            --text-grey: #4B5563;
            --bg-grid-color: rgba(99, 102, 241, 0.1);
        }

        body {
            background: linear-gradient(180deg, #CBB6FC 0%, #EBE5FF 30%, #FFF5F5 100%);
            min-height: 100vh;
            position: relative;
            padding-bottom: 50px;
        }

        /* --- BACKGROUND GRID PATTERN --- */
        .grid-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: linear-gradient(var(--bg-grid-color) 1px, transparent 1px), linear-gradient(90deg, var(--bg-grid-color) 1px, transparent 1px);
            background-size: 60px 60px;
            z-index: -1;
            pointer-events: none;
        }

        /* --- CONTAINER --- */
        .container {
            max-width: 1250px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* --- NAVBAR --- */
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(255, 255, 255, 0.25);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            padding: 15px 30px;
            border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.5);
            margin-top: 20px;
            margin-bottom: 60px;
        }

        .logo {
            font-size: 24px;
            font-weight: 700;
            color: #000;
            display: flex;
            align-items: center;
            gap: 8px;
        }

            /* Logo icon simulation */
            .logo i {
                color: var(--primary-purple);
            }

        .nav-links {
            display: flex;
            gap: 30px;
            list-style: none;
        }

            .nav-links a {
                text-decoration: none;
                color: #000;
                font-weight: 500;
            }

                .nav-links a.active {
                    color: var(--primary-purple);
                }

        .btn-nav {
            background-color: var(--primary-purple);
            color: white;
            padding: 10px 24px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            font-size: 14px;
        }

        /* --- MAIN CONTENT LAYOUT --- */
        .contact-wrapper {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            gap: 60px;
        }

        /* --- LEFT SIDE: TEXT --- */
        .contact-info {
            flex: 0 0 45%;
            padding-top: 20px;
        }

            .contact-info h1 {
                font-size: 48px;
                font-weight: 700;
                color: #312E81; /* Deep Purple */
                line-height: 1.2;
                margin-bottom: 20px;
            }

            .contact-info h3 {
                font-size: 20px;
                font-weight: 500;
                color: #4B5563;
                margin-bottom: 30px;
            }

        /* Feature List */
        .feature-list {
            list-style: none;
            margin-bottom: 40px;
        }

        .feature-item {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 15px;
            color: #4B5563;
            font-size: 16px;
        }

        .icon-circle {
            width: 24px;
            height: 24px;
            background-color: rgba(99, 102, 241, 0.2);
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .icon-dot {
            width: 10px;
            height: 10px;
            background-color: var(--primary-purple);
            border-radius: 50%;
        }

        .trust-text {
            font-size: 14px;
            color: #6B7280;
            line-height: 1.5;
            margin-bottom: 20px;
        }

        /* Review Avatars */
        .reviews {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 30px;
        }

        .avatar-group {
            display: flex;
        }

        .avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            border: 2px solid #fff;
            margin-left: -10px; /* Overlap effect */
        }

            .avatar:first-child {
                margin-left: 0;
            }

        .review-text {
            font-weight: 500;
            color: #4B5563;
            font-size: 14px;
        }

        /* Action Buttons */
        .action-buttons {
            display: flex;
            gap: 20px;
        }

        .btn-demo {
            background-color: var(--primary-purple);
            color: white;
            border: none;
            padding: 14px 28px;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 16px;
        }

        .arrow-icon {
            background: rgba(255,255,255,0.9);
            color: var(--primary-purple);
            width: 18px;
            height: 18px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 10px;
        }

        .btn-contact-sales {
            background: transparent;
            color: var(--primary-purple);
            border: 1px solid #C7D2FE;
            padding: 14px 28px;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            font-size: 16px;
        }

        /* --- RIGHT SIDE: FORM --- */
        .contact-form-card {
            flex: 1;
            background: white;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.08);
        }

        .form-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 20px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

            .form-group label {
                font-size: 14px;
                color: #374151;
                font-weight: 500;
            }

            .form-group input,
            .form-group select,
            .form-group textarea {
                background-color: #F3F4F6;
                border: 1px solid transparent;
                border-radius: 8px;
                padding: 12px 16px;
                font-size: 14px;
                outline: none;
                transition: 0.2s;
                width: 100%;
            }

                .form-group input:focus,
                .form-group select:focus,
                .form-group textarea:focus {
                    background-color: white;
                    border-color: var(--primary-purple);
                }

            .form-group.full-width {
                grid-column: span 2;
            }

            .form-group textarea {
                resize: none;
                height: 120px;
            }

        .btn-submit {
            background-color: var(--primary-purple);
            color: white;
            border: none;
            width: 100%;
            padding: 14px;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            margin-top: 10px;
            transition: background 0.3s;
        }

            .btn-submit:hover {
                background-color: var(--primary-hover);
            }

        /* Responsive */
        @@media (max-width: 1024px) {
            .contact-wrapper

        {
            flex-direction: column;
        }

        .contact-info, .contact-form-card {
            width: 100%;
            flex: none;
        }

        .navbar {
            display: none;
        }
        /* Hide navbar on mobile for simplicity, or add burger menu */
        .contact-info h1 {
            font-size: 36px;
        }

        }

        @@media (max-width: 600px) {
            .form-grid

        {
            grid-template-columns: 1fr;
        }

        .form-group.full-width {
            grid-column: span 1;
        }

        .action-buttons {
            flex-direction: column;
        }

        .btn-demo, .btn-contact-sales {
            width: 100%;
            justify-content: center;
        }

        }
        /* Section Layout */
        .testimonial-section {
            background-color: #fcfcfc; /* Matches the light background */
            padding: 60px 20px;
            font-family: sans-serif; /* Or your preferred font like 'Inter' */
            text-align: center;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        /* Title */
        .section-title {
            color: #5e4b8b; /* Purple color from image */
            font-size: 32px;
            font-weight: 700;
            margin-bottom: 50px;
        }

        /* Slider Layout */
        .testimonial-slider {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 40px;
            margin-bottom: 20px;
        }

        /* Arrows */
        .nav-arrow {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: #fff;
            border: none;
            box-shadow: 0 4px 15px rgba(0,0,0,0.05);
            cursor: pointer;
            font-size: 16px;
            color: #333;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: transform 0.2s;
        }

            .nav-arrow:hover {
                transform: scale(1.05);
            }

        /* Review Card (Bubble) */
        .testimonial-body {
            max-width: 700px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .review-card {
            background: #fff;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.05);
            position: relative;
            margin-bottom: 45px; /* Space for the arrow tail and profile */
        }

        .stars {
            color: #ffce31; /* Star yellow color */
            font-size: 18px;
            margin-bottom: 15px;
            letter-spacing: 3px;
        }

        .review-text {
            font-size: 18px;
            line-height: 1.6;
            color: #333;
            margin: 0;
        }

        /* The Speech Bubble Triangle */
        .card-tail {
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%) rotate(45deg);
            width: 20px;
            height: 20px;
            background: #fff;
            box-shadow: 4px 4px 4px rgba(0,0,0,0.02); /* Subtle shadow on bottom right */
            z-index: 1;
        }

        /* Client Profile */
        .client-info {
            position: relative;
            z-index: 2;
            margin-top: -10px;
        }

        .client-img {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            overflow: hidden;
            margin: 0 auto 15px auto;
            border: 3px solid #fff;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }

            .client-img img {
                width: 100%;
                height: 100%;
                object-fit: cover;
            }

        .client-name {
            font-size: 18px;
            font-weight: 700;
            color: #000;
            margin: 0 0 5px 0;
        }

        .client-role {
            font-size: 14px;
            color: #6c757d;
            margin: 0;
        }

        /* Pagination Dots */
        .dots-wrapper {
            display: flex;
            justify-content: center;
            gap: 8px;
            margin-top: 20px;
        }

        .dot {
            width: 8px;
            height: 8px;
            background-color: #ddd;
            border-radius: 50%;
            cursor: pointer;
        }

            .dot.active {
                background-color: #666;
            }

        /* Responsive */
        @@media (max-width: 768px) {
            .nav-arrow

        {
            display: none;
        }

        .review-card {
            padding: 25px;
        }

        .section-title {
            font-size: 24px;
        }

        }
        /* Main Section Styling */
        .qadryn-contact-section {
            padding: 80px 20px;
            background-color: #ffffff;
            font-family: sans-serif; /* Use your website's font here */
        }

        .q-container {
            max-width: 1140px;
            margin: 0 auto;
        }

        /* Header Styling */
        .q-header {
            text-align: center;
            max-width: 800px;
            margin: 0 auto 60px auto;
        }

        .q-title {
            color: #4C3B8D; /* Exact Deep Purple from image */
            font-size: 32px;
            font-weight: 700;
            margin-bottom: 20px;
        }

        .q-desc {
            color: #4a5568;
            font-size: 16px;
            line-height: 1.6;
            margin: 0;
        }

        /* Grid Layout */
        .q-grid {
            display: flex;
            justify-content: center;
            gap: 30px;
            flex-wrap: wrap;
        }

        /* Card Styling */
        .q-card {
            background-color: #F8F9FE; /* The light bluish/white background */
            border-radius: 10px;
            padding: 50px 20px;
            flex: 1;
            min-width: 280px;
            max-width: 360px;
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            transition: 0.3s;
        }

            .q-card:hover {
                transform: translateY(-5px);
            }

        /* Icon Box Styling */
        .q-icon-box {
            width: 60px;
            height: 60px;
            background-color: #EAE6F6; /* Light Purple square background */
            color: #7A66CC; /* Icon color */
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            margin-bottom: 20px;
        }

        /* Text and Links inside Card */
        .q-card-title {
            color: #2D3748;
            font-size: 20px;
            font-weight: 600;
            margin: 0 0 10px 0;
        }

        .q-link {
            color: #7A66CC; /* Matches Icon color */
            font-weight: 500;
            text-decoration: none;
            font-size: 16px;
        }

            .q-link:hover {
                text-decoration: underline;
            }

        /* Mobile Responsive */
        @@media (max-width: 768px) {
            .q-grid

        {
            flex-direction: column;
            align-items: center;
        }

        .q-card {
            width: 100%;
            max-width: 100%;
        }

        }
    </style>
</head>
<body>

    <!-- Background Grid -->
    <div class="grid-overlay"></div>

    <div class="container">

        <!-- Navbar -->
        <nav class="navbar">
            <div class="logo">
                <i class="fa-solid fa-layer-group"></i> Qadryn
            </div>
            <ul class="nav-links">
                <li><a href="#">Home</a></li>
                <li><a href="#">Features</a></li>
                <li><a href="#">Pricing</a></li>
                <li><a href="#">Devices</a></li>
                <li><a href="#">Blog</a></li>
                <li><a href="#" style="color:var(--primary-purple)">Contact</a></li>
            </ul>
            <a href="#" class="btn-nav">Get Started For Free</a>
        </nav>

        <!-- Main Content -->
        <div class="contact-wrapper">

            <!-- Left Side -->
            <div class="contact-info">
                <h1>Experience Efficiency <br> With Qadryn</h1>
                <h3>We're happy to answer your questions.</h3>

                <ul class="feature-list">
                    <li class="feature-item">
                        <div class="icon-circle"><div class="icon-dot"></div></div>
                        Secure a Appointment To Talk With One Of Our Sale Representative.
                    </li>
                    <li class="feature-item">
                        <div class="icon-circle"><div class="icon-dot"></div></div>
                        Live Chat Support available 24/7
                    </li>
                </ul>

                <p class="trust-text">
                    Trusted, 100% Secure. To Book a Time To Learn More About Qadryn<br>
                    Please Complete The Contact Form Or Call Us Now At +880 4567321
                </p>

                <div class="reviews">
                    <div class="avatar-group">
                        <img src="https://i.pravatar.cc/100?img=5" alt="User" class="avatar">
                        <img src="https://i.pravatar.cc/100?img=3" alt="User" class="avatar">
                        <img src="https://i.pravatar.cc/100?img=12" alt="User" class="avatar">
                        <img src="https://i.pravatar.cc/100?img=9" alt="User" class="avatar">
                    </div>
                    <span class="review-text">100+ Reviews</span>
                </div>

                <div class="action-buttons">
                    <button class="btn-demo">
                        Request a demo
                        <span class="arrow-icon"><i class="fa-solid fa-arrow-up-right-from-square" style="font-size: 8px;"></i></span>
                    </button>
                    <button class="btn-contact-sales">Contact Sales</button>
                </div>
            </div>

            <!-- Right Side: Form -->
            <div class="contact-form-card">
                <form>
                    <div class="form-grid">
                        <!-- Full Name -->
                        <div class="form-group">
                            <label>Full Name</label>
                            <input type="text" placeholder="Full Name">
                        </div>
                        <!-- Last Name -->
                        <div class="form-group">
                            <label>Last Name</label>
                            <input type="text" placeholder="Last Name">
                        </div>
                        <!-- Job Email -->
                        <div class="form-group">
                            <label>Job Email</label>
                            <input type="email" placeholder="Job Email">
                        </div>
                        <!-- Phone Number -->
                        <div class="form-group">
                            <label>Phone Number</label>
                            <input type="tel" placeholder="Phone Number">
                        </div>

                        <!-- Company Size -->
                        <div class="form-group full-width">
                            <label>Company Size</label>
                            <select>
                                <option>Select company size</option>
                                <option>1-10 Employees</option>
                                <option>11-50 Employees</option>
                                <option>50+ Employees</option>
                            </select>
                        </div>

                        <!-- Message -->
                        <div class="form-group full-width">
                            <label>Message</label>
                            <textarea placeholder="Your message here..."></textarea>
                        </div>
                    </div>

                    <button type="submit" class="btn-submit">Send Message</button>
                </form>
            </div>

        </div>
    </div>
    <section class="testimonial-section">
        <div class="container">
            <!-- Section Title -->
            <h2 class="section-title">Kind Words From Our Clients</h2>

            <div class="testimonial-slider">
                <!-- Left Arrow -->
                <button class="nav-arrow prev">
                    <i class="fa-solid fa-chevron-left"></i>
                </button>

                <!-- Main Content -->
                <div class="testimonial-body">

                    <!-- The Speech Bubble Card -->
                    <div class="review-card">
                        <div class="stars">
                            <i class="fa-solid fa-star"></i>
                            <i class="fa-solid fa-star"></i>
                            <i class="fa-solid fa-star"></i>
                            <i class="fa-solid fa-star"></i>
                            <i class="fa-solid fa-star"></i>
                        </div>
                        <p class="review-text">
                            "I've been consistently impressed with the quality of service provided by this website.
                            They have exceeded my expectations and delivered exceptional results. Highly recommended!"
                        </p>
                        <!-- The little triangle at the bottom -->
                        <div class="card-tail"></div>
                    </div>

                    <!-- User Profile -->
                    <div class="client-info">
                        <div class="client-img">
                            <!-- Replace src with your specific image -->
                            <img src="~/image/Icon Strategy.png" alt="Client Photo">
                        </div>
                        <h3 class="client-name">Muzharul Akh (Riad)</h3>
                        <p class="client-role">CEO of Managing Director, Automotive Software</p>
                    </div>

                </div>

                <!-- Right Arrow -->
                <button class="nav-arrow next">
                    <i class="fa-solid fa-chevron-right"></i>
                </button>
            </div>

            <!-- Pagination Dots -->
            <div class="dots-wrapper">
                <span class="dot active"></span>
                <span class="dot"></span>
            </div>
        </div>
    </section>
    <section class="qadryn-contact-section">
        <div class="q-container">
            <!-- Header -->
            <div class="q-header">
                <h2 class="q-title">Contact Us About Qadryn Software</h2>
                <p class="q-desc">
                    We'd love to talk on how we can work together. Write a message and we'll respond as soon as possible. You can also send us an email or call us at the regular address below. We look forward to hearing from you!
                </p>
            </div>

            <!-- Cards Row -->
            <div class="q-grid">

                <!-- Card 1 -->
                <div class="q-card">
                    <div class="q-icon-box">
                        <!-- Replace class below with your icon class or use <img> -->
                        <i class="fa-solid fa-phone-volume"></i>
                    </div>
                    <h3 class="q-card-title">Call Us</h3>
                    <a href="tel:+8801701626626" class="q-link">+880 17 0162 6626</a>
                </div>

                <!-- Card 2 -->
                <div class="q-card">
                    <div class="q-icon-box">
                        <i class="fa-solid fa-user-group"></i>
                    </div>
                    <h3 class="q-card-title">Talk to our team</h3>
                    <a href="#" class="q-link">Chat with us</a>
                </div>

                <!-- Card 3 -->
                <div class="q-card">
                    <div class="q-icon-box">
                        <i class="fa-solid fa-file-lines"></i>
                    </div>
                    <h3 class="q-card-title">Read More</h3>
                    <a href="#" class="q-link">Know More</a>
                </div>

            </div>
        </div>
    </section>
    <!-- === TRUSTED PARTNERS TEXT SECTION === -->
    <section class="partners-text-section text-center">
        <div class="container">
            <div class="mb-3">
                <span class="badge-outlined">Trusted partners</span>
            </div>
            <h2 class="big-heading">
                Managing people made effortless.<br>
                Because great teams deserve great systems.
            </h2>

        </div>
    </section>
</body>
</html>